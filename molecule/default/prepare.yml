---
- name: Prepare
  hosts: all
  gather_facts: true
  become: true
  tasks:
    - name: Install unzip
      ansible.builtin.package:
        name: unzip
        state: present
        update_cache: true
